<UserControl x:Class="Modern.Lab.Controls.Wpf.Display.ModernSlotMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="clr-namespace:Modern.Lab.Controls.Wpf.Common"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="360" d:DesignHeight="480">
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- ControlParts를 병합해 공용 모던 툴팁(암시적 ToolTip 스타일)을
                 그리드 등 다른 컨트롤과 **하나의 정의**로 공유한다. ControlParts가
                 Tokens도 함께 병합하므로 토큰 리소스도 그대로 쓸 수 있다. -->
            <ResourceDictionary.MergedDictionaries>
                <common:SharedResourceDictionary Source="/Modern.Lab.Commons;component/Themes/ControlParts.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- 슬롯 맵: 구획/셀 시각 트리는 코드(SetSections)가 만든다 — 셀 수와
         배치가 데이터에 따라 달라 XAML 템플릿보다 코드 생성이 단순하다. -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Background="Transparent"
                  Focusable="False">
        <StackPanel x:Name="SectionHost" Margin="2" />
    </ScrollViewer>
</UserControl>
