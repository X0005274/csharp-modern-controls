<UserControl x:Class="Modern.Lab.Controls.Wpf.Display.ModernSummaryListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True"
             x:Name="RootControl"
             FontFamily="{DynamicResource Font.Family}"
             d:DesignWidth="320" d:DesignHeight="88">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Modern.Lab.Commons;component/Themes/Tokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Card surface holding a title and a wrapping chip list laid out in one
         row (title left, chips right). Flat=True drops the chrome for use on a
         shared card panel. -->
    <Border>
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="{StaticResource Radius.Lg}" />
                <Setter Property="Background" Value="{StaticResource Brush.Surface}" />
                <Setter Property="BorderBrush" Value="{StaticResource Brush.BorderSubtle}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="16,8" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Flat, ElementName=RootControl}" Value="True">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <DockPanel VerticalAlignment="Center">
            <TextBlock DockPanel.Dock="Left"
                       Text="{Binding Title, ElementName=RootControl}"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource Text.Label}">
                        <Style.Triggers>
                            <!-- Hide the caption entirely when no title is set -->
                            <DataTrigger Binding="{Binding Title, ElementName=RootControl}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ItemsControl x:Name="ChipItems" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="{StaticResource Radius.Pill}"
                                Background="{StaticResource Brush.NeutralBackground}"
                                Padding="10,2"
                                Margin="0,1,6,1">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Label}"
                                           FontSize="{StaticResource Font.Size.Helper}"
                                           Foreground="{StaticResource Brush.NeutralText}"
                                           VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Count}"
                                           FontSize="{StaticResource Font.Size.Helper}"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource Brush.SelectedText}"
                                           Margin="6,0,0,0"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>
    </Border>
</UserControl>
